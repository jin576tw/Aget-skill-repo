# 任務進度總覽

> 依專案與單號整理目前任務進度；`/save` 時僅更新本次 session 主要專案區塊。

Last Updated: 2026-05-18

## POS

- `BMPPOS-22` 休假管理時間編輯狀態：已合併至 `releasev1.0.7`，待補測報。
- `BMPPOS-21` 地址正規化：已完成開發，預計併入 `releasev1.0.9`，待補測報。
- `BMPPOS-14` 建檔作業新增檢核：已完成開發，預計併入 `releasev1.0.8`，待補上傳測報。
- `BMPPOS-20` 變更文件調整：已完成開發，預計併入 `releasev1.0.8`，待補上傳測報。
- `BMPPOS-23` 前掃受理系統日：已併入 `releasev1.0.7` 並上傳測報，待 User 測試。
- `BMPPOS-12` 已併入 `releasev1.0.7` 並上傳測報，待 User 測試。
- `BMPPOS-19` 任務分配：已完成開發。
- `BMPPOS-18` 原 BMPPOS-4 後半段未完成項目：駐列，待 Q3 ASD 部門協助。

### POS Release 進度

| Release | 單號 | 進度 |
| --- | --- | --- |
| `releasev1.0.7` | `BMPPOS-12`、`BMPPOS-23` | 已部署 UAT，待 User 測試 |
| `releasev1.0.8` | `BMPPOS-14`、`BMPPOS-20` | 開發完成，待補上傳測報 |
| `releasev1.0.9` | `BMPPOS-21` | 開發完成，待補測報 |

## Core

- `BMPPCORE-280` 病例公函新增需求：待確認需求與後續排程。

## PA

- `BMPPA-4` 受理人員查詢調整：已完成開發，待補測報。

## ESP

- `ESP-ANALYSIS` 繳費證明查詢與列印：進行分析中，需確認是否與既有模組相關。
- `ESP-ANALYSIS` 分析結果：需由 TBS 與 Kenny 協助驗證。
- `ESP-ANALYSIS` 分析流程：需先梳理正確程式流程，再決定是否往上游依賴模組擴展。
- `ESP-ANALYSIS` 輸出要求：需標示人工推測項目、補充推測追蹤流程，並區分 UI 與 API 分析方式。